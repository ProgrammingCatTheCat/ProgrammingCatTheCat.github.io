---
title: "My Third Post"
date: 2024-03-12T19:16:40+08:00
categories:
- category
- subcategory
tags:
- hugo
- 个人网站
keywords:
- tech
#thumbnailImage: //example.com/image.jpg
---

<!--more-->

# 个人网站搭建教程（3）

> 这一章使我们个人网站搭建的最终章，我们现在就开始吧！



## 05 git本地库关联远程库

首先，我们要在ProgrammingCatTheCat/public中初始化git库。

之后网页的提交也是先在本地更改，本地文件中的public文件也自动更改，git上只提交public文件夹内容。



那么为了创建出public文件，我们需要先构建这个结构，代码为：

`hugo -F --cleanDestinationDir`

也可以只写一个`hugo`,但是带-F的那个是指public每次更改后都清空重建，更加’干净‘一些。



这样之后我们的文件中就有了public。

现在我们先进入public目录，然后输入`git init` 初始化Git库。

![gitInit](..\pictures\gitInit.jpg)



在ProgrammingCatTheCat/public目录下，为git本地库添加远程库：

```bash
git remote add origin git@github.com:ProgrammingCatTheCat/ProgrammingCatTheCat.github.io.git
```



![gitRemote](..\pictures\gitRemote.jpg)



然后查看config文件：

`cat .git/config`

如果返回值里有[remote "origin"]，那么说明本地库已经成功关联远程库啦！



![gitConfig](..\pictures\gitConfig.jpg)

 

## 06 提交修改

首先，我们需要把修改提交到本地库上。

代码如下：

`git status #查看修改`

`git add . #添加所有修改`

`git commit -m "my first commit" #提交修改到本地库，commit备注为“my first commit”`



然后，我们把本地的commi提交到远程库，代码为：

`git push -u origin master`

这个时候，我们可以在github的项目上看到推送的branch，代表我们的博客配置完成了！



不过别急，还有一步。

github创建的项目默认branch是main，但是我们提交的branch是master。

这里我们就需要在settings的page页面里修改默认branch，如图：

![modifyBranch](..\pictures\modifyBranch.jpg)



然后就请打开自己的网站浏览成果吧，网站名就是上图显示的“Your site is live at...”那一块的网址，可以点击View site按钮直达。



如果显示结果和预览的不一样，那么你可以修改hugo.toml中的baseURL为自己的网址，如图所示：

![baseUrl](E:\MyBlog\ProgrammingCatTheCat-Blog\content\pictures\baseUrl.jpg)



好的，那么这次的个人网站教程彻底结束啦，祝你一切顺利！
